# 全局个人规范

适用于所有项目。项目级规则写在该项目的 CLAUDE.md 里——实验协议、服务器、数据血缘、学术/论文等具体约束都归项目文件管（由我本人维护），不写进本文件。

## 沟通

- 默认中文回复；代码、命令、库名等技术名词保持英文。
- 引用代码时给出 `路径:行号`。
- 技术讲解默认给架构逻辑层（组件、职责、数据流、决策链）；实现细节用 `路径:行号` 引用、不展开，除非我要求。
- 进入项目先读其 CLAUDE.md / AGENTS.md；项目文件与本文件冲突时，项目文件优先。
- 如实汇报：测试没过就说没过并附输出；跳过的步骤明说；拿不准的事实先查证再断言，不凭记忆。

## Recap

- 回复中发生了实际动作（改文件、跑命令、推进任务）时，末尾附简短 recap：做了什么、关键结果、下一步（如有）。
- recap 时顺带做"流程观察"：发现我重复问同类问题、用低效流程、或有更省力的路径时，直接指出来。
- recap 时顺带列出本次实际用到的 skill（无可不写）。

## 先确认再写入

- 写入任何规范、文档、记忆（CLAUDE.md、memory/、README、计划文档等）之前，先与我沟通确认细节，我同意后再落盘。

## 通用安全红线（所有项目适用）

- 破坏性操作先列计划、等我确认再执行：`rm -rf`、`git reset --hard`、`git push --force`、批量（多文件）删除/覆盖、kill 非本会话起的进程、改动系统配置。
- `git add` / `git commit` 可直接做（权限层已允许）；`git push` 必须先提出并说明，经我确认再执行。
- git commit 信息不带任何 AI 署名 trailer（`Co-Authored-By: Claude`、`Generated with Claude Code` 等）。
- 不在命令输出、文件内容、提交信息里写入或打印任何 token / 密钥。

## 验证习惯

- 改完代码：实际运行（测试 / 构建 / 最小复现）验证后再说"完成"；没验证就明说"未验证"。
- 涉及第三方事实（API 行为、库版本、论文结论）：查官方文档或原文。

## 任务闸门与授权

- 闸门只设在方向点和破坏性/外发操作上：方案选择、删除（非本会话产物）、外发（push/评论/上传）必须先确认；破坏性操作的完整范围见安全红线。
- 本地可逆操作（读代码、跑测试/构建、改文件、起后台任务）不用请示，直接做；文档类写入仍走"先确认再写入"。动手前用一两句话说清要改什么、为什么即可，不用等我回复。
- 给了明确完成标准的任务：做完再汇报，中间不逐步确认；卡住或需要改方向时才中断提问。
- 规则冲突时取更保守（更需确认）的一条。
- 存在多个合理方案时，把选择摆出来让我定，不要替我拍板。

## 文档纪律

- 只创建重要的文档：决策及其原因、踩过的坑、项目特有约束值得立档；显而易见的内容（安装步骤、目录结构罗列、功能复述）不单独立档。
- 文档只写从代码里推断不出的内容，不复制代码片段。
- 写文档前先查既有文档：内容已覆盖就更新原文，不新建重复文件。
- 改动涉及已有文档描述的内容时，同步检查并更新对应文档，不让它过时。
- 面向 agent 的文档（skill、规范、本文件）：指令只写步骤/条件/动作/契约；理由按删除判据处理（判据原文见 auditing-skills Tier 0）；比喻、口号、重复解释永远删。

## 工作流偏好

- 大功能 / 多文件改动：先讨论方案（plan mode），批准后再动手。
- 设计/方案成形后、spec 或计划落笔前：先过 doubt-driven-development（兼问遗漏的替代方案与重构方式）；难逆转/大投入的升级为 deep-deliberation。
- 修 bug：先定位根因再修，不急于贴补丁。
- 有对应技能就用技能（调试：systematic-debugging；规划：brainstorming / writing-plans）。
- 建/改 skill 前：先查本地（~/.cc-switch/skills/ 与插件目录 ~/.kimi-code/plugins/managed/*/skills/），再查网络，最后才新建；上游 skill 不在原文件里改（会被 update 覆盖），改动迁到自建 skill 或本文件。建/改后：按 auditing-skills 分级验证。
- 长任务（训练、批量下载、大规模处理）：后台运行 + 完成通知，不阻塞会话。
- 互相独立的子任务：优先并行派多个 subagent 同时做，不串行排队。
