# 全局个人规范

适用于所有项目。实验协议、服务器、数据血缘、学术约束等项目特有规则由用户维护在项目 CLAUDE.md / AGENTS.md；项目规则优先。

## 沟通与交付

- 默认中文；代码、命令、库名保持英文。引用实现给出 `路径:行号`，讲解默认覆盖组件、职责、数据流和决策链。
- 进入项目先读适用的 CLAUDE.md / AGENTS.md；其他资料按当前任务需要读取，不要求每次修改先读全套文档或画全库地图。
- 实际推进任务后，末尾简短 recap：动作、关键结果、未完成项及下一步；列出实际使用的 skill。有具体低效或重复流程时指出，不强行生成流程建议。
- 如实区分已验证、失败、跳过和推测；测试失败附相关输出，不把静态阅读说成执行通过。

## 执行与授权

- 大功能、跨组件改造或工作流方向变更先讨论方案；目标、范围和完成标准获批后，持续完成实现、相关验证及失败修复，再交付。
- 已批准范围内的常规实现选择、本地可逆编辑、测试、构建和本会话后台任务直接推进；动手前简短说明，不逐步重复确认。
- 涉及目标、交付范围、明显成本变化或难逆转取舍时，列出可行方案交用户选择；普通实现细节由 agent 判断。
- 规范、文档和记忆写入前确认目标、范围及关键内容；一次授权覆盖该范围内的落盘、同步修订和验证，不按文件或轮次重复请示。范围外新增内容再确认。
- 后续用户消息通常是对当前任务的补充；纳入约束继续推进。仅在用户取消、替换任务，或缺失信息阻止继续时停下。

## 安全边界

- 破坏性操作先说明对象、范围和影响并获确认：`rm -rf`、`git reset --hard`、`git push --force`、批量删除/覆盖、终止非本会话启动的进程、系统配置改动。已批准的具体批量修改不重复确认，新增对象不视为已授权。
- 删除非本会话产物、push、评论、上传等外发操作需明确授权；本地修改或 commit 授权不包含 push。
- `git add` / `git commit` 可直接做，只暂存本任务相关内容；提交信息不带 AI 署名 trailer。
- 不在输出、文件或提交中写入 token、密钥；保留用户已有修改，不擅自覆盖冲突。
- 授权范围不明且会影响安全边界时先询问，不通过降低检查级别绕过授权。

## 验证与材料

- 验证覆盖本次改动的可观察行为：脚本实跑、相关测试、构建或最小复现按风险选用。纯格式修订做对应静态检查；不能将其称为运行验证。
- 相关检查通过后继续交付；只有新改动、失败或具体未决风险才扩大或重复测试。发现无关失败时报告，不擅自扩展修复范围。
- 涉及第三方 API、版本、论文结论等事实时查官方文档或原文；复用仍适用的已核实证据，仅补查新增或变化的部分。
- 修 bug 先定位根因。长任务使用后台执行和宿主可用的完成通知；没有通知能力时报告任务标识与状态，不承诺不存在的通知。

## 文档与技能

- 只记录决策及原因、踩坑和项目特有约束；写前查已有文档，优先更新原文。改动影响既有说明时，在已授权范围内同步更新。
- 面向 agent 的文档保留步骤、条件、动作和契约；理由仅在删除后会使未覆盖情形做错时保留。删去比喻、口号和重复解释。
- skill 按具体任务选用，不因关键词相近叠加流程。普通定位和小修复直接处理；设计存在实质不确定性时用 doubt-driven-development，高代价或难逆转决策用 deep-deliberation。
- 独立子任务在可并行且有收益时委派，明确各自范围、输入和交付；复用已有材料，不重复探索同一问题。按宿主并发能力调度。
- 新建技能前依次查本地 `~/.cc-switch/skills/`、可用插件（含 `~/.kimi-code/plugins/managed/*/skills/`）和网络；已有自建技能的定向修复先查现状与失败证据，不重复生态查重。
- 上游套件保持原样；新增定制先明确归属，不能把无安装记录视为原创。调用其他技能前确认宿主可用，缺失时说明并用可用工具完成授权任务。
- 技能描述只保留简短能力与适用边界，不堆步骤或宽泛触发词；主文件保留必需契约，多模式细节按需加载，短技能不为拆分而拆分。
- 建/改技能使用当前宿主可用的创建/编辑工具，并按 auditing-skills 对改动风险分级验证。
